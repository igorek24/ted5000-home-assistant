{
  "config": {
    "step": {
      "user": {
        "title": "TED5000 gateway",
        "description": "Enter the IP address of your TED5000 gateway (the box on your network, not the display). Credentials are only needed if you enabled security in Footprints.",
        "data": {
          "host": "Host or IP address",
          "port": "Port",
          "username": "Username (optional)",
          "password": "Password (optional)"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach the gateway or it returned no MTU data",
      "invalid_auth": "The gateway rejected those credentials"
    },
    "abort": {
      "already_configured": "This TED5000 gateway is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "TED5000 options",
        "data": {
          "scan_interval_seconds": "Polling interval in seconds",
          "create_circuit_energy": "Create energy sensors for each submetered circuit (needed for per-device tracking in the Energy dashboard)",
          "phantom_days": "Phantom load: number of days to average",
          "phantom_window_start": "Phantom load: quiet window start (HH:MM)",
          "phantom_window_end": "Phantom load: quiet window end (HH:MM)"
        }
      }
    }
  }
}